server:
  port: 8083

spring:
  application:
    name: ZOMATO-AUTHENTICATION-SERVICE
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/